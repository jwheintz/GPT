# Classroom Control - Local Relay Requirements
# Install with: pip install -r requirements.txt

# Supabase client for real-time command listening
supabase>=2.0.0

# OBS WebSocket control (OBS 28+ has built-in WebSocket server)
obsws-python>=1.6.0

# System tray icon and notifications (Windows)
pystray>=0.19.0
Pillow>=10.0.0

# For creating standalone Windows executable
pyinstaller>=6.0.0

# Configuration management
python-dotenv>=1.0.0

# HTTP requests (dependency, but explicit)
httpx>=0.25.0

# WebSocket support
websockets>=12.0
